apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hds-tm-events.fullname" . }}-test-connection"
  labels:
    {{- include "hds-tm-events.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hds-tm-events.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
